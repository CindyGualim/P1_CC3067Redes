[pytest]
pythonpath = src
testpaths = tests
asyncio_mode = auto

# Textual closes its event loop while the MCP child processes are still being
# collected; the resulting ResourceWarning is noise, not a leak in our code.
filterwarnings =
    ignore::ResourceWarning
    ignore::pytest.PytestUnraisableExceptionWarning
