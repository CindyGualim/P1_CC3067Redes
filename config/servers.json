{
  "$comment": "MCP servers the chatbot connects to. ${PYTHON}, ${PROJECT_ROOT} and ${WORKSPACE} are expanded at load time so the file works on any machine.",
  "servers": [
    {
      "name": "pharmacy",
      "description": "Servidor propio: inventario, recetas y ordenes de compra de la cadena Farmacia Vida.",
      "command": ["${PYTHON}", "${PROJECT_ROOT}/src/servers/pharmacy/__main__.py"],
      "env": {
        "PYTHONIOENCODING": "utf-8",
        "PYTHONUTF8": "1"
      },
      "enabled": true
    },
    {
      "name": "filesystem",
      "description": "Servidor oficial Filesystem (@modelcontextprotocol/server-filesystem), limitado al directorio workspace/.",
      "command": ["npx", "-y", "@modelcontextprotocol/server-filesystem", "${WORKSPACE}"],
      "enabled": true
    },
    {
      "name": "git",
      "description": "Servidor oficial Git (paquete mcp-server-git). Opera sobre los repositorios creados dentro de workspace/.",
      "command": ["${PYTHON}", "-m", "mcp_server_git"],
      "enabled": true
    }
  ]
}
